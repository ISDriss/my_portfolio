# ideas

1. gravity/universe sandbox,
2. rainy, ad filled, town square (cuberpunk/Time square)
- The ads would be carousels of past projects,
- one ad billboard by type of projects with main billboard being the CV,
- could add random events,

3. retain some formal aspects of some of those very pro portfolios, aka : keep it "clean"

# tasklist

1 - color palette
for now, let's consider the following:
 #FFFFFF 
 #FFE100
 #FF6403
 #494341
 #030E3C

2 - get models

3 - design scene
4 - make a formal landing page


# instals made

```bash
npm i three
npm install tailwindcss @tailwindcss/postcss postcss

npm install lucide-react
npm i radix-ui/react-slot class-variance-authority
```